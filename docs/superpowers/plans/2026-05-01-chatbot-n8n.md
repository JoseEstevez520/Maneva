# Chatbot IA via n8n — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conectar el chat de la app a un agente n8n con DeepSeek-V3 que conversa, recomienda salones y peluqueros, y devuelve tarjetas interactivas con botón de reserva.

**Architecture:** La app envía `{session_id, message}` al webhook de n8n. n8n ejecuta un AI Agent (DeepSeek-V3) con Simple Memory (memoria por usuario) y dos herramientas HTTP que consultan Supabase REST (salon_locations y employees). El system prompt fuerza JSON estructurado `{reply, salons[], stylists[]}`. Un Code node parsea la respuesta antes de devolverla. La app renderiza tarjetas; "Reservar" navega a la pantalla de reservas con el salón preseleccionado.

**Tech Stack:** React Native, Expo Router, NativeWind, n8n AI Agent, DeepSeek-V3 (OpenAI-compatible), Supabase REST API.

---

## Ficheros que cambian

| Fichero | Acción | Responsabilidad |
|---|---|---|
| `maneva/src/services/ai.service.ts` | Modificar | Tipos actualizados + payload con session_id sin history |
| `maneva/src/hooks/useChat.ts` | Crear | Estado y lógica del chat (extraído de chat.tsx) |
| `maneva/app/chat.tsx` | Modificar | Usar useChat, tarjeta StylistCard, botón Reservar |
| `docs/n8n-workflow.json` | Crear | Workflow importable en n8n |
| `maneva/supabase/seed-chat.sql` | Crear | Datos de prueba para salones y ubicaciones |

---

## Task 1: Actualizar ai.service.ts

**Files:**
- Modify: `maneva/src/services/ai.service.ts`

- [ ] **Paso 1: Reemplazar el contenido completo del fichero**

```typescript
/**
 * ai.service.ts
 * Capa de comunicación con el webhook de n8n.
 */

// ─── Tipos ────────────────────────────────────────────────────────────────────

export type SalonSuggestion = {
  id: string
  name: string
  city?: string
  photo_url?: string | null
}

export type StylistSuggestion = {
  id: string
  display_name: string
  specialty: string | null
  photo_url: string | null
  salon_name: string
  salon_id: string
  location_id: string
}

export type ChatResponse = {
  reply: string
  salons: SalonSuggestion[]
  stylists: StylistSuggestion[]
}

type ChatPayload = {
  session_id: string
  message: string
  user_id: string
}

// ─── Función principal ─────────────────────────────────────────────────────────

export async function sendChatMessage(
  message: string,
  sessionId: string,
): Promise<ChatResponse> {
  const webhookUrl = process.env.EXPO_PUBLIC_N8N_WEBHOOK_URL

  if (!webhookUrl) {
    await new Promise<void>((resolve) => setTimeout(resolve, 1200))
    return MOCK_RESPONSE
  }

  const payload: ChatPayload = {
    session_id: sessionId,
    message,
    user_id: sessionId,
  }

  const response = await fetch(webhookUrl, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  })

  if (!response.ok) {
    throw new Error(`Error del asistente (${response.status})`)
  }

  return response.json() as Promise<ChatResponse>
}

// ─── Mock para desarrollo ─────────────────────────────────────────────────────

const MOCK_RESPONSE: ChatResponse = {
  reply:
    'Hola, soy el asistente de Maneva. Configura el webhook de n8n en .env para activarme. ¡Estoy listo!',
  salons: [],
  stylists: [],
}
```

- [ ] **Paso 2: Verificar TypeScript**

```bash
cd maneva && npx tsc --noEmit 2>&1 | head -20
```

Esperado: sin errores relacionados con `ai.service.ts`.

- [ ] **Paso 3: Commit**

```bash
git add maneva/src/services/ai.service.ts
git commit -m "feat(chat): actualizar ai.service con session_id y StylistSuggestion"
```

---

## Task 2: Crear useChat.ts

**Files:**
- Create: `maneva/src/hooks/useChat.ts`

- [ ] **Paso 1: Crear el fichero**

```typescript
/**
 * useChat.ts
 * Gestiona el estado de la conversación con el asistente IA.
 * Extrae la lógica que antes vivía en app/chat.tsx.
 */
import { useState, useCallback } from 'react'
import { useAuthStore } from '@/store/authStore'
import {
  sendChatMessage,
  type SalonSuggestion,
  type StylistSuggestion,
} from '@/services/ai.service'

// ─── Tipos ────────────────────────────────────────────────────────────────────

export type ChatMessageRole = 'user' | 'bot'

export type ChatMessage = {
  id: string
  role: ChatMessageRole
  content: string
  salons?: SalonSuggestion[]
  stylists?: StylistSuggestion[]
}

// ─── Hook ─────────────────────────────────────────────────────────────────────

export function useChat() {
  const { user } = useAuthStore()
  const [messages, setMessages] = useState<ChatMessage[]>([])
  const [isTyping, setIsTyping] = useState(false)

  const sendMessage = useCallback(
    async (text: string) => {
      const trimmed = text.trim()
      if (!trimmed || isTyping) return

      const sessionId = user?.id ?? 'anonymous'

      const userMessage: ChatMessage = {
        id: Date.now().toString(),
        role: 'user',
        content: trimmed,
      }

      setMessages((prev) => [...prev, userMessage])
      setIsTyping(true)

      try {
        const response = await sendChatMessage(trimmed, sessionId)

        const botMessage: ChatMessage = {
          id: (Date.now() + 1).toString(),
          role: 'bot',
          content: response.reply,
          salons: response.salons,
          stylists: response.stylists,
        }

        setMessages((prev) => [...prev, botMessage])
      } catch {
        const errorMessage: ChatMessage = {
          id: (Date.now() + 1).toString(),
          role: 'bot',
          content: 'Lo siento, ha ocurrido un error. Inténtalo de nuevo.',
        }
        setMessages((prev) => [...prev, errorMessage])
      } finally {
        setIsTyping(false)
      }
    },
    [isTyping, user?.id],
  )

  return { messages, isTyping, sendMessage }
}
```

- [ ] **Paso 2: Verificar TypeScript**

```bash
cd maneva && npx tsc --noEmit 2>&1 | head -20
```

Esperado: sin errores.

- [ ] **Paso 3: Commit**

```bash
git add maneva/src/hooks/useChat.ts
git commit -m "feat(chat): crear hook useChat con session_id y soporte stylists"
```

---

## Task 3: Actualizar chat.tsx

**Files:**
- Modify: `maneva/app/chat.tsx`

- [ ] **Paso 1: Reemplazar el bloque de imports (líneas 1-38)**

```typescript
/**
 * chat.tsx
 * Pantalla del asistente IA. Se presenta como fullScreenModal desde el FAB.
 */
import React, { useState, useRef, useEffect } from 'react'
import {
  View,
  TextInput,
  ScrollView,
  KeyboardAvoidingView,
  Platform,
  TouchableOpacity,
  Pressable,
  Image,
} from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import { useRouter } from 'expo-router'
import Animated, {
  FadeInDown,
  FadeInUp,
  useAnimatedStyle,
  useSharedValue,
  withRepeat,
  withSequence,
  withSpring,
  withTiming,
  withDelay,
} from 'react-native-reanimated'
import { useThemeColors } from '@/hooks/useThemeColors'
import { Body, Caption, H2 } from '@/components/ui/Typography'
import {
  IconClose,
  IconSend,
  IconSparkles,
  IconLocation,
  IconScissors,
} from '@/components/ui/icons'
import { useChat, type ChatMessage } from '@/hooks/useChat'
import type { StylistSuggestion } from '@/services/ai.service'
```

- [ ] **Paso 2: Eliminar el bloque de tipos locales (líneas 41-68 del fichero original)**

Eliminar todo el bloque entre `// ─── Tipos locales` y `// ─── Chips de acceso rápido`:

```typescript
// ─── Tipos locales ─────────────────────────────────────────────────────────────

type MessageRole = 'user' | 'bot'

type Message = {
  id: string
  role: MessageRole
  content: string
  salons?: SalonSuggestion[]
}

// TODO: Agente conversacional rico — mensajes tipados
// ...todo el bloque de comentario TODO...
```

- [ ] **Paso 3: Añadir el componente StylistCard justo antes de `// Declarado a nivel de módulo`**

Insertar después del cierre de `QuickChips` (la función que termina con `}`):

```typescript
/** Tarjeta de peluquero sugerido por el bot */
function StylistCard({ stylist }: { stylist: StylistSuggestion }) {
  const router = useRouter()
  const themeColors = useThemeColors()

  return (
    <TouchableOpacity
      onPress={() => router.push(`/(tabs)/bookings?salonId=${stylist.salon_id}`)}
      activeOpacity={0.8}
      className="bg-surface dark:bg-surface-dark border border-border dark:border-border-dark-strong rounded-xl px-4 py-3 flex-row items-center gap-3"
    >
      {stylist.photo_url ? (
        <Image
          source={{ uri: stylist.photo_url }}
          className="w-10 h-10 rounded-full"
        />
      ) : (
        <View className="w-10 h-10 rounded-full bg-[rgba(212,175,55,0.15)] items-center justify-center">
          <IconScissors size={16} color={themeColors.gold.DEFAULT} strokeWidth={2} />
        </View>
      )}
      <View className="flex-1">
        <Caption className="font-manrope-extrabold text-[12px] text-foreground dark:text-foreground-dark">
          {stylist.display_name}
        </Caption>
        {stylist.specialty && (
          <Caption className="font-manrope-medium text-[11px] text-foreground-muted dark:text-foreground-muted-dark">
            {stylist.specialty}
          </Caption>
        )}
        <Caption className="font-manrope-medium text-[10px] text-foreground-muted dark:text-foreground-muted-dark">
          {stylist.salon_name}
        </Caption>
      </View>
      <View className="bg-premium-black dark:bg-foreground-dark rounded-lg px-3 py-1.5">
        <Caption className="font-manrope-bold text-[11px] text-premium-white dark:text-surface-dark">
          Reservar
        </Caption>
      </View>
    </TouchableOpacity>
  )
}
```

- [ ] **Paso 4: Reemplazar la función MessageBubble entera**

Sustituir la función `MessageBubble` completa (desde `function MessageBubble` hasta su `}` de cierre) por:

```typescript
/** Burbuja de mensaje — usuario (derecha, negro) o bot (izquierda, gris claro) */
function MessageBubble({ message }: { message: ChatMessage }) {
  const isUser = message.role === 'user'
  const router = useRouter()
  const themeColors = useThemeColors()

  return (
    <Animated.View
      entering={isUser ? FadeInUp.duration(250) : FadeInDown.duration(250)}
      className={`mb-3 max-w-[82%] ${isUser ? 'self-end mr-5' : 'self-start ml-5'}`}
    >
      <View
        className={`px-4 py-3 ${
          isUser
            ? 'bg-premium-black rounded-2xl rounded-tr-sm'
            : 'bg-surface-raised dark:bg-surface-raised-dark rounded-2xl rounded-tl-sm'
        }`}
      >
        <Body
          className={`font-manrope-medium text-[14px] leading-[20px] ${
            isUser ? 'text-premium-white' : 'text-foreground dark:text-foreground-dark'
          }`}
        >
          {message.content}
        </Body>
      </View>

      {message.salons && message.salons.length > 0 && (
        <View className="mt-2 gap-2">
          {message.salons.map((salon) => (
            <TouchableOpacity
              key={salon.id}
              onPress={() => router.push(`/(tabs)/bookings?salonId=${salon.id}`)}
              activeOpacity={0.8}
              className="bg-surface dark:bg-surface-dark border border-border dark:border-border-dark-strong rounded-xl px-4 py-3 flex-row items-center gap-3"
            >
              <View className="w-8 h-8 rounded-full bg-[rgba(212,175,55,0.15)] items-center justify-center">
                <IconSparkles size={14} color={themeColors.gold.DEFAULT} strokeWidth={2} />
              </View>
              <View className="flex-1">
                <Caption className="font-manrope-extrabold text-[12px] text-foreground dark:text-foreground-dark">
                  {salon.name}
                </Caption>
                {salon.city && (
                  <Caption className="font-manrope-medium text-[11px] text-foreground-muted dark:text-foreground-muted-dark">
                    {salon.city}
                  </Caption>
                )}
              </View>
              <View className="bg-premium-black dark:bg-foreground-dark rounded-lg px-3 py-1.5">
                <Caption className="font-manrope-bold text-[11px] text-premium-white dark:text-surface-dark">
                  Reservar
                </Caption>
              </View>
            </TouchableOpacity>
          ))}
        </View>
      )}

      {message.stylists && message.stylists.length > 0 && (
        <View className="mt-2 gap-2">
          {message.stylists.map((stylist) => (
            <StylistCard key={stylist.id} stylist={stylist} />
          ))}
        </View>
      )}
    </Animated.View>
  )
}
```

- [ ] **Paso 5: Reemplazar la función ChatScreen entera**

Sustituir todo desde `export default function ChatScreen()` hasta el final del fichero por:

```typescript
export default function ChatScreen() {
  const themeColors = useThemeColors()
  const router = useRouter()
  const { messages, isTyping, sendMessage } = useChat()
  const [input, setInput] = useState('')
  const scrollRef = useRef<ScrollView>(null)

  const sendScale = useSharedValue(1)
  const sendAnimatedStyle = useAnimatedStyle(() => ({ transform: [{ scale: sendScale.value }] }))

  useEffect(() => {
    setTimeout(() => scrollRef.current?.scrollToEnd({ animated: true }), 100)
  }, [messages, isTyping])

  const handleSend = (text: string) => {
    setInput('')
    sendMessage(text)
  }

  const canSend = input.trim().length > 0 && !isTyping

  return (
    <SafeAreaView className="flex-1 bg-surface dark:bg-surface-dark" edges={['top', 'bottom']}>
      {/* ── Header ── */}
      <Animated.View
        entering={FadeInDown.duration(300)}
        className="flex-row items-center justify-between px-5 py-4 border-b border-border dark:border-border-dark"
      >
        <View className="flex-row items-center gap-3">
          <View className="w-9 h-9 rounded-full bg-surface dark:bg-surface-dark border border-gold items-center justify-center">
            <IconSparkles size={16} color={themeColors.gold.DEFAULT} strokeWidth={2} />
          </View>
          <View>
            <H2 className="font-manrope-extrabold text-[16px] text-foreground dark:text-foreground-dark leading-[20px]">
              Asistente Maneva
            </H2>
            <Caption className="font-manrope-medium text-[11px] text-foreground-muted dark:text-foreground-muted-dark">
              IA · Responde en segundos
            </Caption>
          </View>
        </View>

        <TouchableOpacity
          onPress={() => router.back()}
          activeOpacity={0.7}
          className="w-8 h-8 rounded-full bg-surface-raised dark:bg-surface-raised-dark items-center justify-center"
        >
          <IconClose size={16} color={themeColors.premium.black} strokeWidth={2.5} />
        </TouchableOpacity>
      </Animated.View>

      {/* ── Mensajes ── */}
      <KeyboardAvoidingView
        className="flex-1"
        behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
        keyboardVerticalOffset={Platform.OS === 'ios' ? 0 : 20}
      >
        <ScrollView
          ref={scrollRef}
          className="flex-1"
          contentContainerClassName="pt-6 pb-4"
          showsVerticalScrollIndicator={false}
        >
          {messages.length === 0 && (
            <Animated.View entering={FadeInDown.duration(400).springify()}>
              <View className="self-start ml-5 mb-3 max-w-[82%]">
                <View className="bg-surface-raised dark:bg-surface-raised-dark px-4 py-3 rounded-2xl rounded-tl-sm">
                  <Body className="font-manrope-medium text-[14px] leading-[20px] text-foreground dark:text-foreground-dark">
                    ¡Hola! Soy el asistente de Maneva. Puedo ayudarte a encontrar salones,
                    recomendar peluqueros o resolver cualquier duda. ¿En qué te ayudo?
                  </Body>
                </View>
              </View>
              <QuickChips onSelect={handleSend} />
            </Animated.View>
          )}

          {messages.map((message) => (
            <MessageBubble key={message.id} message={message} />
          ))}

          {isTyping && <TypingIndicator />}
        </ScrollView>

        {/* ── Input ── */}
        <View className="flex-row items-end gap-3 px-5 py-3 border-t border-border dark:border-border-dark">
          <TextInput
            className="flex-1 bg-surface-raised dark:bg-surface-raised-dark rounded-2xl px-4 py-3 font-manrope-medium text-[14px] text-foreground dark:text-foreground-dark max-h-[100px]"
            placeholder="Escribe tu pregunta..."
            placeholderTextColor={themeColors.premium.gray.DEFAULT}
            value={input}
            onChangeText={setInput}
            multiline
            onSubmitEditing={() => handleSend(input)}
            blurOnSubmit={false}
          />

          <AnimatedPressable
            onPress={() => handleSend(input)}
            onPressIn={() => {
              if (canSend) sendScale.value = withSpring(0.88, { damping: 15, stiffness: 300 })
            }}
            onPressOut={() => {
              sendScale.value = withSpring(1, { damping: 15, stiffness: 300 })
            }}
            disabled={!canSend}
            style={sendAnimatedStyle}
            className={`w-11 h-11 rounded-full items-center justify-center ${
              canSend ? 'bg-foreground dark:bg-foreground-dark' : 'bg-border dark:bg-border-dark'
            }`}
          >
            <IconSend
              size={18}
              color={canSend ? themeColors.premium.white : themeColors.premium.gray.DEFAULT}
              strokeWidth={2}
            />
          </AnimatedPressable>
        </View>
      </KeyboardAvoidingView>
    </SafeAreaView>
  )
}
```

- [ ] **Paso 6: Verificar TypeScript**

```bash
cd maneva && npx tsc --noEmit 2>&1 | head -30
```

Esperado: sin errores. `SalonSuggestion` se infiere automáticamente de `ChatMessage.salons` — no hace falta importarlo explícitamente en `chat.tsx`.

- [ ] **Paso 7: Commit**

```bash
git add maneva/app/chat.tsx
git commit -m "feat(chat): refactorizar con useChat, StylistCard y botón Reservar"
```

---

## Task 4: Workflow n8n

**Files:**
- Create: `docs/n8n-workflow.json`

- [ ] **Paso 1: Crear el fichero JSON del workflow**

```json
{
  "name": "Maneva Chatbot",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "maneva-chat",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "c1a2b3d4-1111-1111-1111-111111111111",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "c1a2b3d4-0000-0000-0000-000000000000"
    },
    {
      "parameters": {
        "agent": "conversationalAgent",
        "promptType": "define",
        "text": "={{ $('Webhook').item.json.message }}",
        "options": {
          "systemMessage": "Eres el asistente IA de Maneva, una app de reservas de peluquería y belleza. Tu personalidad es amigable, cercana y profesional.\n\nPuedes:\n- Conversar de forma natural sobre peluquería, belleza y tendencias capilares\n- Recomendar salones usando la herramienta buscar_salones\n- Recomendar peluqueros/estilistas usando la herramienta buscar_peluqueros\n\nCuando el usuario mencione una ciudad concreta, úsala para filtrar. Cuando mencione una especialidad (coloración, corte, barba, etc.), úsala para filtrar peluqueros.\n\nREGLA CRÍTICA: Tu respuesta SIEMPRE debe ser ÚNICAMENTE un JSON válido con exactamente esta estructura. Sin texto adicional. Sin markdown. Solo el JSON:\n{\"reply\": \"Tu mensaje aquí\", \"salons\": [], \"stylists\": []}\n\nCuando incluyas salones en 'salons', cada objeto debe tener:\n{\"id\": \"uuid\", \"name\": \"nombre\", \"city\": \"ciudad\", \"photo_url\": null}\n\nCuando incluyas peluqueros en 'stylists', cada objeto debe tener:\n{\"id\": \"uuid\", \"display_name\": \"nombre profesional\", \"specialty\": \"especialidad\", \"photo_url\": null, \"salon_name\": \"nombre del salón\", \"salon_id\": \"uuid del salón\", \"location_id\": \"uuid de la ubicación\"}\n\nSi solo conversas sin recomendar nada, deja ambos arrays vacíos []."
        }
      },
      "id": "c1a2b3d4-2222-2222-2222-222222222222",
      "name": "AI Agent",
      "type": "@n8n/n8n-nodes-langchain.agent",
      "typeVersion": 1.7,
      "position": [500, 300]
    },
    {
      "parameters": {
        "model": "deepseek-chat",
        "options": {
          "temperature": 0.7,
          "maxTokens": 1000
        }
      },
      "id": "c1a2b3d4-3333-3333-3333-333333333333",
      "name": "DeepSeek Chat Model",
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1.2,
      "position": [360, 520],
      "credentials": {
        "openAiApi": {
          "id": "REEMPLAZAR_CON_ID_CREDENCIAL_DEEPSEEK",
          "name": "DeepSeek API"
        }
      }
    },
    {
      "parameters": {
        "sessionKey": "={{ $('Webhook').item.json.session_id }}",
        "contextWindowLength": 10
      },
      "id": "c1a2b3d4-4444-4444-4444-444444444444",
      "name": "Simple Memory",
      "type": "@n8n/n8n-nodes-langchain.memoryBufferWindow",
      "typeVersion": 1.3,
      "position": [500, 520]
    },
    {
      "parameters": {
        "description": "Busca salones de peluquería y belleza en la base de datos. Úsala cuando el usuario quiera encontrar un salón, pregunte por salones en una ciudad, o pida recomendaciones de salones.",
        "method": "GET",
        "url": "https://REEMPLAZAR_PROJECT_ID.supabase.co/rest/v1/salon_locations",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "select",
              "value": "id,name,city,salon_id,salons(id,name,logo)"
            },
            {
              "name": "active",
              "value": "eq.true"
            },
            {
              "name": "limit",
              "value": "20"
            }
          ]
        },
        "optimizeResponse": true
      },
      "id": "c1a2b3d4-5555-5555-5555-555555555555",
      "name": "buscar_salones",
      "type": "@n8n/n8n-nodes-langchain.toolHttpRequest",
      "typeVersion": 1.1,
      "position": [640, 520],
      "credentials": {
        "httpHeaderAuth": {
          "id": "REEMPLAZAR_CON_ID_CREDENCIAL_SUPABASE",
          "name": "Supabase Anon Key"
        }
      }
    },
    {
      "parameters": {
        "description": "Busca peluqueros y estilistas en la base de datos. Úsala cuando el usuario pregunte por peluqueros, quiera encontrar un especialista concreto (coloración, corte, barba, etc.) o pida recomendaciones de profesionales.",
        "method": "GET",
        "url": "https://REEMPLAZAR_PROJECT_ID.supabase.co/rest/v1/employees",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "select",
              "value": "id,bio,position,specialties,photo_url,location_id,salon_locations(id,city,salon_id,salons(id,name))"
            },
            {
              "name": "active",
              "value": "eq.true"
            },
            {
              "name": "limit",
              "value": "20"
            }
          ]
        },
        "optimizeResponse": true
      },
      "id": "c1a2b3d4-6666-6666-6666-666666666666",
      "name": "buscar_peluqueros",
      "type": "@n8n/n8n-nodes-langchain.toolHttpRequest",
      "typeVersion": 1.1,
      "position": [780, 520],
      "credentials": {
        "httpHeaderAuth": {
          "id": "REEMPLAZAR_CON_ID_CREDENCIAL_SUPABASE",
          "name": "Supabase Anon Key"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const agentOutput = $input.first().json.output || '';\n\nlet parsed;\ntry {\n  const jsonMatch = agentOutput.match(/\\{[\\s\\S]*\\}/);\n  parsed = JSON.parse(jsonMatch ? jsonMatch[0] : agentOutput);\n} catch {\n  parsed = { reply: agentOutput, salons: [], stylists: [] };\n}\n\nreturn [{\n  json: {\n    reply: typeof parsed.reply === 'string' ? parsed.reply : agentOutput,\n    salons: Array.isArray(parsed.salons) ? parsed.salons : [],\n    stylists: Array.isArray(parsed.stylists) ? parsed.stylists : [],\n  }\n}];"
      },
      "id": "c1a2b3d4-7777-7777-7777-777777777777",
      "name": "Formatear respuesta",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [760, 300]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              }
            ]
          }
        }
      },
      "id": "c1a2b3d4-8888-8888-8888-888888888888",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1000, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [{ "node": "AI Agent", "type": "main", "index": 0 }]
      ]
    },
    "AI Agent": {
      "main": [
        [{ "node": "Formatear respuesta", "type": "main", "index": 0 }]
      ]
    },
    "Formatear respuesta": {
      "main": [
        [{ "node": "Respond to Webhook", "type": "main", "index": 0 }]
      ]
    },
    "DeepSeek Chat Model": {
      "ai_languageModel": [
        [{ "node": "AI Agent", "type": "ai_languageModel", "index": 0 }]
      ]
    },
    "Simple Memory": {
      "ai_memory": [
        [{ "node": "AI Agent", "type": "ai_memory", "index": 0 }]
      ]
    },
    "buscar_salones": {
      "ai_tool": [
        [{ "node": "AI Agent", "type": "ai_tool", "index": 0 }]
      ]
    },
    "buscar_peluqueros": {
      "ai_tool": [
        [{ "node": "AI Agent", "type": "ai_tool", "index": 1 }]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false,
  "tags": []
}
```

- [ ] **Paso 2: Importar en n8n**

1. Abrir n8n → "Workflows" → botón "+" → "Import from file"
2. Seleccionar `docs/n8n-workflow.json`
3. El workflow se importa con nodos marcados en rojo (credenciales pendientes)

- [ ] **Paso 3: Crear credencial DeepSeek**

1. n8n → Settings → Credentials → "Add Credential" → buscar "OpenAI"
2. Nombre: `DeepSeek API`
3. API Key: tu clave de DeepSeek
4. Base URL: `https://api.deepseek.com/v1`
5. Guardar y asignar al nodo "DeepSeek Chat Model"

- [ ] **Paso 4: Crear credencial Supabase**

1. n8n → Settings → Credentials → "Add Credential" → buscar "Header Auth"
2. Nombre: `Supabase Anon Key`
3. Name (header): `apikey`
4. Value: tu `SUPABASE_ANON_KEY`
5. Guardar y asignar a los nodos "buscar_salones" y "buscar_peluqueros"

- [ ] **Paso 5: Añadir header Authorization a los nodos HTTP**

En cada nodo HTTP (buscar_salones y buscar_peluqueros), en la sección "Headers", añadir manualmente:
- Header: `Authorization`
- Value: `Bearer TU_SUPABASE_ANON_KEY`

- [ ] **Paso 6: Reemplazar la URL de Supabase en los nodos HTTP**

En "buscar_salones", cambiar URL:
```
https://TU_PROJECT_ID.supabase.co/rest/v1/salon_locations
```

En "buscar_peluqueros", cambiar URL:
```
https://TU_PROJECT_ID.supabase.co/rest/v1/employees
```

- [ ] **Paso 7: Activar el workflow y copiar la URL del webhook**

1. Activar el workflow (toggle en esquina superior derecha)
2. Hacer clic en el nodo "Webhook" → copiar "Production URL"
3. Pegar en `maneva/.env`:
   ```
   EXPO_PUBLIC_N8N_WEBHOOK_URL=https://tu-n8n.com/webhook/maneva-chat
   ```

- [ ] **Paso 8: Prueba rápida del webhook**

```bash
curl -X POST https://tu-n8n.com/webhook/maneva-chat \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test-123","message":"Hola, busco un salón en Madrid","user_id":"test-123"}'
```

Esperado: respuesta JSON con `reply`, `salons` y `stylists`.

- [ ] **Paso 9: Commit**

```bash
git add docs/n8n-workflow.json
git commit -m "feat(chat): añadir workflow n8n para el chatbot IA"
```

---

## Task 5: Datos de prueba (seed)

**Files:**
- Create: `maneva/supabase/seed-chat.sql`

> **Nota:** La tabla `employees` requiere `user_id` con FK a `auth.users`. Para añadir peluqueros de prueba, crear primero los usuarios desde el Dashboard de Supabase (Authentication → Users → "Invite user") con rol `stylist`, copiar sus UUIDs y usarlos abajo.

- [ ] **Paso 1: Crear el fichero SQL**

```sql
-- ─── Datos de prueba para el chatbot ─────────────────────────────────────────
-- Ejecutar en Supabase Dashboard → SQL Editor
-- Los IDs son fijos para facilitar pruebas repetibles.

-- Salones
INSERT INTO salons (id, name, description)
VALUES
  ('11111111-0000-0000-0000-000000000001', 'Salón Elite', 'Peluquería de lujo en el centro'),
  ('11111111-0000-0000-0000-000000000002', 'Studio Belleza', 'Especialistas en coloración y tendencias'),
  ('11111111-0000-0000-0000-000000000003', 'Hair Lab', 'Peluquería artesanal con 15 años de experiencia')
ON CONFLICT (id) DO NOTHING;

-- Ubicaciones (con ciudad — que es lo que usa el chatbot)
INSERT INTO salon_locations (id, salon_id, name, city, address, active)
VALUES
  ('22222222-0000-0000-0000-000000000001', '11111111-0000-0000-0000-000000000001', 'Salón Elite Madrid', 'Madrid', 'Calle Gran Vía 45', true),
  ('22222222-0000-0000-0000-000000000002', '11111111-0000-0000-0000-000000000002', 'Studio Belleza Barcelona', 'Barcelona', 'Carrer de Verdi 12', true),
  ('22222222-0000-0000-0000-000000000003', '11111111-0000-0000-0000-000000000003', 'Hair Lab Sevilla', 'Sevilla', 'Calle Sierpes 8', true)
ON CONFLICT (id) DO NOTHING;

-- Empleados (peluqueros)
-- IMPORTANTE: Sustituir los UUIDs de user_id por UUIDs reales de auth.users
-- Crear usuarios desde: Supabase Dashboard → Authentication → Users → Invite user
-- INSERT INTO employees (id, user_id, location_id, position, specialties, bio, active)
-- VALUES
--   ('33333333-0000-0000-0000-000000000001', 'UUID_REAL_USER_1', '22222222-0000-0000-0000-000000000001',
--    'María García', 'Coloración, Balayage, Mechas', 'Especialista en color con 8 años de experiencia', true),
--   ('33333333-0000-0000-0000-000000000002', 'UUID_REAL_USER_2', '22222222-0000-0000-0000-000000000001',
--    'Carlos Ruiz', 'Corte, Barba, Fade', 'Barbero especializado en estilos modernos', true),
--   ('33333333-0000-0000-0000-000000000003', 'UUID_REAL_USER_3', '22222222-0000-0000-0000-000000000002',
--    'Laura Sánchez', 'Tratamientos, Keratina, Hidratación', 'Experta en tratamientos capilares', true)
-- ON CONFLICT (id) DO NOTHING;
```

- [ ] **Paso 2: Ejecutar el SQL en Supabase**

1. Abrir Supabase Dashboard → SQL Editor
2. Pegar el contenido del fichero
3. Ejecutar
4. Verificar que hay 3 filas en `salons` y 3 en `salon_locations`

- [ ] **Paso 3: Verificar que el chatbot encuentra los salones**

Lanzar la app y abrir el chat. Escribir: "Busco un salón en Madrid".

Esperado:
- El bot responde con texto
- Aparece la tarjeta de "Salón Elite Madrid" con botón "Reservar"

- [ ] **Paso 4: Añadir empleados de prueba (opcional)**

Si quieres probar las recomendaciones de peluqueros:
1. Supabase Dashboard → Authentication → Users → "Invite user" (3 usuarios)
2. Copiar los UUIDs generados
3. Descomentar el bloque de employees en el SQL, sustituir los UUIDs y ejecutar
4. En el chat, escribir: "Busco un colorista en Madrid"

- [ ] **Paso 5: Commit**

```bash
git add maneva/supabase/seed-chat.sql
git commit -m "feat(chat): añadir script seed con datos de prueba para el chatbot"
```

---

## Verificación final

- [ ] La app compila sin errores TypeScript (`npx tsc --noEmit`)
- [ ] El chat muestra el mensaje de bienvenida y los chips de acceso rápido
- [ ] Sin webhook configurado: la respuesta mock aparece tras ~1.2 segundos
- [ ] Con webhook configurado: el bot responde con salones/peluqueros cuando corresponde
- [ ] El botón "Reservar" de las tarjetas navega a `/(tabs)/bookings`
- [ ] La memoria funciona: el bot recuerda lo que dijiste antes en la misma sesión
